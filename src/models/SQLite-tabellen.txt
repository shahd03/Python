CREATE TABLE boeken (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    titel TEXT NOT NULL,
    auteur TEXT NOT NULL,
    jaar INTEGER NOT NULL
);

CREATE TABLE klanten (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    naam TEXT NOT NULL,
    email TEXT NOT NULL
);
